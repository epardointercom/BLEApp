<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:BLEApp;assembly=BLEApp"
             x:Class="BLEApp.Views.SelectedDevice"
             Title="Dispositivo seleccionado">
    <ContentPage.Content>
        <StackLayout Padding="20,5,20,5">
            <Label Text="Id Dispositivo:"
                   Margin="0,10,0,0"/>
            <Label Text="{Binding Id}"
                   Margin="10,0,0,0" />
            <Label Text="Nombre:"
                   Margin="0,10,0,0" />
            <Label Text="{Binding Name}"
                   Margin="10,0,0,0" />
            <Label Text="Descripción:"
                   Margin="0,10,0,0" />
            <Label Text="{Binding Description}"
                   Margin="10,0,0,0" />
            <Label Text="Temperatúra actual:" 
                   Margin="0,10,0,0"/>
            <Label Text="{Binding Temperature}"
                   Margin="10,0,0,0" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>